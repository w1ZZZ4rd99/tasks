# Banking System (OOP)

A modular banking platform built iteratively with object-oriented design (DDD-style domain
models).

## Requirements

- Python 3.10+
- `pytest` (for tests) — `pip install -r requirements.txt`

## Run

```bash
python -m src.main      # run the demo
python -m pytest        # run the tests
```
