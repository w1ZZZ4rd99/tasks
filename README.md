# Banking System — Day 7

## Run

    python -m venv .venv
    .venv/bin/pip install -r requirements.txt
    .venv/bin/python -m src.main           # all demos
    .venv/bin/python -m src.simulation     # full simulation
    .venv/bin/python -m src.reporting      # reports + charts
    .venv/bin/python -m pytest             # tests
    .venv/bin/jupyter notebook demo.ipynb           # main demos notebook
    .venv/bin/jupyter notebook reports_demo.ipynb   # reporting & charts notebook
